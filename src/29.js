function getRandomInt(max) {
  return Math.floor(Math.random() * max);
}

// Example usage:
const maxValue = 100;
console.log(getRandomInt(maxValue)); // Output a random integer between 0 and maxValue-1
