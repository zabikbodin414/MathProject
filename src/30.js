function squareRoot(num) {
    const sqrt = Math.sqrt(num);
    console.log(`The square root of ${num} is approximately ${sqrt.toFixed(2)}`);
}

squareRoot(16); // Replace 16 with another number to test the function
