// Code Example 1
function addNumbers(a, b) {
    return a + b;
}

// Code Example 2
const values = [3, 4, 5];
console.log(`Sum of ${values[0]} and ${values[1]} is: ${addNumbers(values[0], values[1])}`);
